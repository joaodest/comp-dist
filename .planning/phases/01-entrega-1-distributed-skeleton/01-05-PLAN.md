---
phase: 01-entrega-1-distributed-skeleton
plan: 05
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - docs/architecture.md
  - docs/messages.md
  - docs/roles.md
  - docs/report/entrega1.tex
  - docs/report/references.bib
  - docs/report/build.ps1
  - docs/report/entrega1.pdf
autonomous: true
requirements:
  - COUR-01
  - COUR-04
  - ARCH-05
must_haves:
  truths:
    - "The project has architecture documentation explaining Gateway, Lobby, Game and their message flow."
    - "The docs list the HTTP routes and gRPC messages implemented in Phase 1."
    - "The first report draft covers problem, architecture, implemented requirements, challenges and student roles."
    - "Student ownership can be filled by squad without inventing a Docs-only squad."
  artifacts:
    - path: "docs/architecture.md"
      provides: "Distributed architecture explanation"
      contains: "Gateway"
    - path: "docs/messages.md"
      provides: "HTTP and gRPC message reference"
      contains: "CreateRoom"
    - path: "docs/roles.md"
      provides: "Squad ownership and report-role placeholders"
      contains: "Squad"
    - path: "docs/report/entrega1.tex"
      provides: "SBC report source"
      contains: "sbc-template"
    - path: "docs/report/entrega1.pdf"
      provides: "Entrega 1 report PDF"
      min_lines: 1
  key_links:
    - from: "docs/messages.md"
      to: "proto/lobby/v1/lobby.proto"
      via: "documents implemented Lobby RPCs"
      pattern: "CreateRoom"
    - from: "docs/messages.md"
      to: "proto/game/v1/game.proto"
      via: "documents implemented Game RPCs"
      pattern: "StartMatch"
    - from: "docs/report/entrega1.tex"
      to: "docs/architecture.md"
      via: "report architecture section summarizes detailed docs"
      pattern: "Gateway.*Lobby.*Game"
    - from: "docs/report/build.ps1"
      to: "docs/report/entrega1.tex"
      via: "builds the SBC PDF artifact"
      pattern: "pdflatex|latexmk"
---

<objective>
Prepare the architecture documentation and first SBC report draft for Entrega 1.

Purpose: Phase 1 is only successful if the group can explain the distributed architecture and submit a credible report, not just run code.
Output: Architecture docs, message docs, role placeholders, SBC LaTeX source, build script, and the target PDF artifact.
</objective>

<execution_context>
@C:/Users/João/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/João/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-CONTEXT.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-RESEARCH.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document architecture and message contracts</name>
  <files>docs/architecture.md, docs/messages.md</files>
  <action>Create architecture documentation with a text diagram showing Browser/curl -> Gateway HTTP -> Lobby gRPC -> Game gRPC and the Docker Compose network. Create message documentation listing each public HTTP route, request/response JSON, each gRPC method, and the purpose of main fields. Keep future WebSocket/gameplay items clearly marked as deferred.</action>
  <verify><automated>Select-String -Path docs/architecture.md,docs/messages.md -Pattern 'Gateway','Lobby','Game','CreateRoom','StartMatch'</automated></verify>
  <done>Docs explain who exchanges messages with whom, what each message contains, and why the design satisfies gRPC plus web services.</done>
</task>

<task type="auto">
  <name>Task 2: Create squad ownership and role placeholders</name>
  <files>docs/roles.md</files>
  <action>Create a 3-squad ownership table for 9 students without a Docs-only squad. Assign functional slices: canonical room-start backend flow, supporting contract/skeleton slice, and demo/report integration slice. Leave explicit placeholders for student names and squad owners, and state that each squad owns its own report section.</action>
  <verify><automated>Select-String -Path docs/roles.md -Pattern 'Squad','Gateway','Lobby','Game','PLACEHOLDER'</automated></verify>
  <done>`docs/roles.md` can be filled by the group before submission and matches the context decision to group roles by squad.</done>
</task>

<task type="auto">
  <name>Task 3: Build the Entrega 1 SBC PDF path</name>
  <files>docs/report/entrega1.tex, docs/report/references.bib, docs/report/build.ps1, docs/report/entrega1.pdf</files>
  <action>Create an SBC-template LaTeX report source in Portuguese with sections for problem, architecture, implemented requirements, implementation details, challenges, and student roles. Keep it concise enough for the required 4-page limit. Add `docs/report/build.ps1` that builds `entrega1.pdf` with `latexmk` or `pdflatex` if available, then verifies the generated PDF exists and has at most 4 pages when `pdfinfo` is available. If a TeX toolchain is missing during execution, the script must fail with clear installation instructions rather than silently skipping PDF generation. Mention AI only as auxiliary if needed; avoid generic AI-sounding filler and make the text reflect the actual running skeleton and docs.</action>
  <verify><automated>pwsh docs/report/build.ps1</automated></verify>
  <done>`docs/report/entrega1.pdf` is generated from SBC LaTeX source, includes required COUR-04 sections, and the build script checks the 4-page limit when local tooling supports page counting.</done>
</task>

</tasks>

<verification>
Run the three task-level document checks plus `pwsh docs/report/build.ps1`. Then manually inspect docs for consistency with current proto names, route names, Compose service names and README demo commands.
</verification>

<success_criteria>
- `COUR-01` is covered by a clear distributed-game problem explanation.
- `COUR-04` is covered by an SBC-source report and generated PDF path with page-limit validation.
- `ARCH-05` is covered by architecture/message docs tied to actual artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entrega-1-distributed-skeleton/01-05-SUMMARY.md`.
</output>
