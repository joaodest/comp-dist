---
phase: 01-entrega-1-distributed-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - cmd/lobby/main.go
  - cmd/game/main.go
  - internal/lobby/store.go
  - internal/lobby/server.go
  - internal/lobby/server_test.go
  - internal/game/store.go
  - internal/game/server.go
  - internal/game/server_test.go
  - internal/ids/ids.go
  - internal/observability/logger.go
autonomous: true
requirements:
  - COUR-02
  - ARCH-02
must_haves:
  truths:
    - "Lobby runs as a separate Go service exposing the generated Lobby gRPC server."
    - "Game runs as a separate Go service exposing the generated Game gRPC server."
    - "Lobby can create, join, inspect and start a room using in-memory state."
    - "Starting a room calls the Game service through gRPC and returns a `match_id`."
    - "Service logs include correlated IDs for demo visibility."
  artifacts:
    - path: "cmd/lobby/main.go"
      provides: "Lobby service process entrypoint"
      contains: "grpc.NewServer"
    - path: "cmd/game/main.go"
      provides: "Game service process entrypoint"
      contains: "grpc.NewServer"
    - path: "internal/lobby/server.go"
      provides: "LobbyServiceServer implementation"
      contains: "StartRoom"
    - path: "internal/game/server.go"
      provides: "GameServiceServer implementation"
      contains: "StartMatch"
  key_links:
    - from: "internal/lobby/server.go"
      to: "internal/contracts/lobby/v1"
      via: "implements generated LobbyServiceServer"
      pattern: "lobbyv1.*UnimplementedLobbyServiceServer"
    - from: "internal/lobby/server.go"
      to: "internal/contracts/game/v1"
      via: "generated GameServiceClient used by StartRoom"
      pattern: "gamev1.*GameServiceClient"
    - from: "cmd/lobby/main.go"
      to: "internal/lobby/server.go"
      via: "server registration"
      pattern: "RegisterLobbyServiceServer"
    - from: "cmd/game/main.go"
      to: "internal/game/server.go"
      via: "server registration"
      pattern: "RegisterGameServiceServer"
---

<objective>
Implement the backend gRPC services that own Phase 1 room and match state.

Purpose: The course requirement is not met by contracts alone; separate Go services must exchange meaningful RPCs.
Output: Lobby and Game gRPC servers with in-memory state, health endpoints, correlated logs, and tests.
</objective>

<execution_context>
@C:/Users/João/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/João/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-CONTEXT.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-RESEARCH.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared ID and logging helpers</name>
  <files>internal/ids/ids.go, internal/observability/logger.go</files>
  <action>Create a small UUID helper for `request_id`, `room_id`, `player_id`, and `match_id`. Create a `slog` JSON logger helper that accepts a service name and supports contextual fields. Keep helpers dependency-light and avoid OpenTelemetry in this phase.</action>
  <verify><automated>go test ./internal/ids/... ./internal/observability/...</automated></verify>
  <done>Helpers compile, generated IDs are non-empty UUID strings, and logger setup is reusable by all service entrypoints.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Game gRPC service</name>
  <files>cmd/game/main.go, internal/game/store.go, internal/game/server.go, internal/game/server_test.go</files>
  <action>Implement `GameServiceServer` with minimal in-memory match state. `StartMatch` must validate `request_id` and `room_id`, create a `match_id`, store room/player IDs, log `request_id`, `room_id`, and `match_id`, and return the match ID. In `cmd/game/main.go`, create a gRPC server, call `RegisterGameServiceServer`, listen on the configured gRPC address, and add a simple HTTP `/healthz` endpoint on a separate port for Compose healthchecks.</action>
  <verify><automated>go test ./internal/game/...</automated></verify>
  <done>Tests prove `StartMatch` creates retrievable match state and returns gRPC status errors for invalid input.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Lobby gRPC service</name>
  <files>cmd/lobby/main.go, internal/lobby/store.go, internal/lobby/server.go, internal/lobby/server_test.go</files>
  <action>Implement `LobbyServiceServer` with mutex-protected in-memory room state. `CreateRoom` returns `room_id` and `join_url`; `JoinRoom` registers one or more players and returns `player_id`; `GetRoom` returns current room state; `StartRoom` validates the room, calls the generated `GameServiceClient.StartMatch`, stores returned `match_id`, and returns it. Add a simple HTTP `/healthz` endpoint for Compose healthchecks.</action>
  <verify><automated>go test ./internal/lobby/...</automated></verify>
  <done>Tests prove create/join/get/start behavior, including a fake Game client proving Lobby triggers Game through the generated interface.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/game/... ./internal/lobby/...` and then `go test ./...`. Confirm Lobby and Game are separate `cmd/` entrypoints and do not call each other through in-process functions.
</verification>

<success_criteria>
- `ARCH-02` is represented by separate Gateway/Lobby/Game service layout, with Lobby and Game implemented here.
- `COUR-02` has a real Lobby -> Game gRPC path.
- Game remains minimal: no gameplay, ticks, arena or snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entrega-1-distributed-skeleton/01-02-SUMMARY.md`.
</output>
