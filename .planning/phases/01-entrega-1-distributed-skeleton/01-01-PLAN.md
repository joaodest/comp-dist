---
phase: 01-entrega-1-distributed-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - Makefile
  - proto/lobby/v1/lobby.proto
  - proto/game/v1/game.proto
  - internal/contracts/lobby/v1/lobby.pb.go
  - internal/contracts/lobby/v1/lobby_grpc.pb.go
  - internal/contracts/game/v1/game.pb.go
  - internal/contracts/game/v1/game_grpc.pb.go
autonomous: true
requirements:
  - COUR-02
  - ARCH-03
must_haves:
  truths:
    - "The repository has a Go module with pinned gRPC, Protobuf and UUID dependencies."
    - "Lobby and Game gRPC contracts exist as `.proto` files."
    - "Generated Go gRPC code exists and compiles."
  artifacts:
    - path: "go.mod"
      provides: "Go module and backend dependency pins"
      contains: "google.golang.org/grpc"
    - path: "proto/lobby/v1/lobby.proto"
      provides: "LobbyService contract for create, join, get and start room"
      contains: "service LobbyService"
    - path: "proto/game/v1/game.proto"
      provides: "GameService contract for starting a match"
      contains: "service GameService"
    - path: "Makefile"
      provides: "Repeatable proto/test/demo commands"
      contains: "proto:"
  key_links:
    - from: "proto/lobby/v1/lobby.proto"
      to: "internal/contracts/lobby/v1/lobby_grpc.pb.go"
      via: "make proto generation command"
      pattern: "protoc.*lobby.proto"
    - from: "proto/game/v1/game.proto"
      to: "internal/contracts/game/v1/game_grpc.pb.go"
      via: "make proto generation command"
      pattern: "protoc.*game.proto"
---

<objective>
Create the contract-first foundation for the distributed skeleton.

Purpose: Phase 1 must prove gRPC/RPC with generated contracts before service behavior is implemented.
Output: Go module, proto contracts, generated Go code, and repeatable Makefile commands.
</objective>

<execution_context>
@C:/Users/João/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/João/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-CONTEXT.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and command surface</name>
  <files>go.mod, go.sum, Makefile</files>
  <action>Create a Go module for the project. Add dependencies for `google.golang.org/grpc`, `google.golang.org/protobuf`, and `github.com/google/uuid`. Add Makefile targets `proto`, `test`, `docker-up`, `docker-down`, and `demo`; the `proto` target must run `protoc` for both Lobby and Game contracts using source-relative output into `internal/contracts/...`.</action>
  <verify><automated>go test ./...</automated></verify>
  <done>`go test ./...` runs successfully in an empty/skeleton module, and `make proto` is documented by the Makefile target.</done>
</task>

<task type="auto">
  <name>Task 2: Define Lobby and Game protobuf contracts</name>
  <files>proto/lobby/v1/lobby.proto, proto/game/v1/game.proto</files>
  <action>Create `LobbyService` with `CreateRoom`, `JoinRoom`, `GetRoom`, and `StartRoom` RPCs. Include `request_id`, `room_id`, `join_url`, `player_id`, `player_name`, and `match_id` fields needed by the canonical flow. Create `GameService` with `StartMatch` and `GetMatch` RPCs. Set explicit `go_package` options under `internal/contracts/...`.</action>
  <verify><automated>make proto</automated></verify>
  <done>Both `.proto` files compile through `make proto`, and generated files appear under `internal/contracts/lobby/v1` and `internal/contracts/game/v1`.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and compile contract code</name>
  <files>internal/contracts/lobby/v1/lobby.pb.go, internal/contracts/lobby/v1/lobby_grpc.pb.go, internal/contracts/game/v1/game.pb.go, internal/contracts/game/v1/game_grpc.pb.go</files>
  <action>Run the Makefile `proto` target and keep generated Go code in the repository so executors for later plans can import stable interfaces. Do not hand-write generated files. If `protoc` or generator binaries are missing, install `protoc-gen-go` and `protoc-gen-go-grpc` with `go install` and document the prerequisite in `Makefile` comments.</action>
  <verify><automated>go test ./...</automated></verify>
  <done>Generated Go contract packages compile and expose `LobbyServiceServer`, `LobbyServiceClient`, `GameServiceServer`, and `GameServiceClient` symbols.</done>
</task>

</tasks>

<verification>
Run `make proto` and `go test ./...`. Inspect generated package imports to confirm later service plans can import `internal/contracts/lobby/v1` and `internal/contracts/game/v1`.
</verification>

<success_criteria>
- `COUR-02` has a concrete gRPC implementation path through generated contracts.
- `ARCH-03` is satisfied by committed `.proto` files for main inter-service messages.
- Later plans can depend on stable generated client/server interfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entrega-1-distributed-skeleton/01-01-SUMMARY.md`.
</output>
