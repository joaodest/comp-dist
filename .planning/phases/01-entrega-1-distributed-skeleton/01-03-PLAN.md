---
phase: 01-entrega-1-distributed-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - cmd/gateway/main.go
  - internal/gateway/handler.go
  - internal/gateway/handler_test.go
  - internal/gateway/grpc_errors.go
  - internal/gateway/types.go
autonomous: true
requirements:
  - COUR-02
  - COUR-03
  - ARCH-04
must_haves:
  truths:
    - "Gateway runs as a separate Go service exposing HTTP JSON web services."
    - "`POST /rooms` creates a room through Lobby gRPC and returns `room_id` plus `join_url`."
    - "`POST /rooms/{room_id}/join` registers a player through Lobby gRPC and returns `player_id`."
    - "`POST /rooms/{room_id}/start` triggers Lobby/Game gRPC flow and returns `match_id`."
    - "Gateway maps gRPC status errors to structured JSON HTTP errors."
  artifacts:
    - path: "cmd/gateway/main.go"
      provides: "Gateway service entrypoint"
      contains: "http.ListenAndServe"
    - path: "internal/gateway/handler.go"
      provides: "HTTP handlers for room flow"
      contains: "POST /rooms"
    - path: "internal/gateway/grpc_errors.go"
      provides: "gRPC to HTTP error mapping"
      contains: "status.Code"
  key_links:
    - from: "internal/gateway/handler.go"
      to: "internal/contracts/lobby/v1"
      via: "generated LobbyServiceClient"
      pattern: "lobbyv1.*LobbyServiceClient"
    - from: "cmd/gateway/main.go"
      to: "internal/gateway/handler.go"
      via: "HTTP mux registration"
      pattern: "HandleFunc"
    - from: "internal/gateway/grpc_errors.go"
      to: "internal/gateway/handler.go"
      via: "shared JSON error writer"
      pattern: "writeGRPCError"
---

<objective>
Expose the required public web services through Gateway and bridge them to Lobby gRPC.

Purpose: Phase 1 must demonstrate both web services and gRPC in one visible flow.
Output: Gateway HTTP API for create/join/start/get room, healthcheck, JSON errors, and tests.
</objective>

<execution_context>
@C:/Users/João/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/João/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-CONTEXT.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-RESEARCH.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-01-SUMMARY.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gateway HTTP entrypoint and route types</name>
  <files>cmd/gateway/main.go, internal/gateway/types.go</files>
  <action>Create Gateway configuration for `HTTP_ADDR` and `LOBBY_ADDR`, initialize a generated Lobby gRPC client, create an `http.ServeMux`, and register `GET /healthz`, `POST /rooms`, `POST /rooms/{room_id}/join`, `POST /rooms/{room_id}/start`, and `GET /rooms/{room_id}`. Define request/response structs with JSON tags matching the README demo commands.</action>
  <verify><automated>go test ./internal/gateway/...</automated></verify>
  <done>Gateway package compiles and exposes a handler constructor that can be tested with fake Lobby clients.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HTTP-to-gRPC room handlers</name>
  <files>internal/gateway/handler.go, internal/gateway/handler_test.go</files>
  <action>Implement the canonical flow exactly from context. Each handler must ensure or generate `request_id`, apply a short outbound gRPC timeout, call the generated Lobby client, log `request_id` and relevant IDs, and return structured JSON. Use Go `net/http` only; do not add Gin/Chi/Echo.</action>
  <verify><automated>go test ./internal/gateway/... -run 'Test.*Room'</automated></verify>
  <done>Handler tests prove create, join, start and get routes call the fake Lobby client with expected fields and return the expected JSON/status codes.</done>
</task>

<task type="auto">
  <name>Task 3: Map gRPC errors to HTTP JSON errors</name>
  <files>internal/gateway/grpc_errors.go, internal/gateway/handler_test.go</files>
  <action>Create a shared JSON error format such as `{ \"error\": \"...\", \"code\": \"...\" }`. Map `InvalidArgument` to 400, `NotFound` to 404, `FailedPrecondition` to 409, `DeadlineExceeded` to 504, and default errors to 500. Add tests for at least invalid join input and missing room.</action>
  <verify><automated>go test ./internal/gateway/... -run 'Test.*Error'</automated></verify>
  <done>Gateway never returns vague plain-text errors for gRPC failures and keeps JSON responses consistent.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/gateway/...` and `go test ./...`. Confirm the Gateway handlers call the generated Lobby client rather than importing Lobby store/server internals.
</verification>

<success_criteria>
- `COUR-03` is satisfied by HTTP JSON web services.
- `COUR-02` remains visible because the HTTP flow calls gRPC clients.
- `ARCH-04` is covered by room/status/health endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entrega-1-distributed-skeleton/01-03-SUMMARY.md`.
</output>
