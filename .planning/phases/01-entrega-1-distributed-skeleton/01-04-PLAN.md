---
phase: 01-entrega-1-distributed-skeleton
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - docker-compose.yml
  - cmd/gateway/Dockerfile
  - cmd/lobby/Dockerfile
  - cmd/game/Dockerfile
  - Makefile
  - README.md
autonomous: true
requirements:
  - COUR-02
  - COUR-03
  - ARCH-01
  - ARCH-02
  - ARCH-04
must_haves:
  truths:
    - "`docker compose up --build` starts Gateway, Lobby and Game as separate services."
    - "Gateway can reach Lobby over the Compose network."
    - "Lobby can reach Game over the Compose network."
    - "The README contains copy-paste curl commands for create, join, start and inspect room."
    - "The demo proves web services and gRPC through one command flow."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local distributed runtime"
      contains: "gateway:"
    - path: "cmd/gateway/Dockerfile"
      provides: "Gateway container build"
      contains: "go build"
    - path: "cmd/lobby/Dockerfile"
      provides: "Lobby container build"
      contains: "go build"
    - path: "cmd/game/Dockerfile"
      provides: "Game container build"
      contains: "go build"
    - path: "README.md"
      provides: "Runbook and demo script"
      contains: "curl"
  key_links:
    - from: "docker-compose.yml"
      to: "cmd/gateway/main.go"
      via: "Gateway service build and exposed HTTP port"
      pattern: "dockerfile: cmd/gateway/Dockerfile"
    - from: "docker-compose.yml"
      to: "cmd/lobby/main.go"
      via: "Lobby service hostname used by Gateway"
      pattern: "LOBBY_ADDR: lobby:"
    - from: "docker-compose.yml"
      to: "cmd/game/main.go"
      via: "Game service hostname used by Lobby"
      pattern: "GAME_ADDR: game:"
---

<objective>
Package the skeleton as a reproducible local distributed demo.

Purpose: The professor and team need to run three networked services and prove web services plus gRPC without manual terminal choreography.
Output: Dockerfiles, Compose topology, healthchecks, Makefile-compatible commands, and README demo steps.
</objective>

<execution_context>
@C:/Users/João/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/João/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-CONTEXT.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-RESEARCH.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-02-SUMMARY.md
@.planning/phases/01-entrega-1-distributed-skeleton/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service Dockerfiles</name>
  <files>cmd/gateway/Dockerfile, cmd/lobby/Dockerfile, cmd/game/Dockerfile</files>
  <action>Create small multi-stage Dockerfiles for each service. Build from the repository root so generated contracts and internal packages are available. Each final image should run only its service binary and expose the relevant HTTP/gRPC ports documented in README.</action>
  <verify><automated>docker compose build</automated></verify>
  <done>Gateway, Lobby and Game Dockerfiles can each build from the repo root without copying unrelated local files such as `.planning` into the final runtime image.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the three-service network</name>
  <files>docker-compose.yml</files>
  <action>Create `gateway`, `lobby`, and `game` services. Gateway exposes host port 8080 and uses `LOBBY_ADDR=lobby:50051`. Lobby exposes gRPC 50051 internally, health HTTP 8081 internally, and uses `GAME_ADDR=game:50052`. Game exposes gRPC 50052 internally and health HTTP 8082 internally. Add healthchecks and use `depends_on.condition: service_healthy` where supported.</action>
  <verify><automated>docker compose config</automated></verify>
  <done>`docker compose config` validates and service hostnames match the environment variables consumed by code.</done>
</task>

<task type="auto">
  <name>Task 3: Document and script the demo flow</name>
  <files>README.md, Makefile</files>
  <action>Update README with prerequisites, `make proto`, `make test`, `docker compose up --build`, and copy-paste curl commands for `POST /rooms`, `POST /rooms/{room_id}/join`, `POST /rooms/{room_id}/start`, and `GET /rooms/{room_id}`. Update Makefile `demo` target to print or run the same canonical commands without requiring a frontend.</action>
  <verify><automated>go test ./...</automated></verify>
  <done>README contains a complete Entrega 1 demonstration path, expected JSON examples, and explains where to look in logs for `request_id`, `room_id`, `player_id`, and `match_id`.</done>
</task>

</tasks>

<verification>
Run `docker compose config`, `go test ./...`, then manually smoke test `docker compose up --build` and the README curl flow if Docker is available.
</verification>

<success_criteria>
- `ARCH-01` is satisfied by multiple networked services.
- `ARCH-02` is demonstrable through three service containers.
- `COUR-02`, `COUR-03`, and `ARCH-04` are demonstrable from README commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-entrega-1-distributed-skeleton/01-04-SUMMARY.md`.
</output>
